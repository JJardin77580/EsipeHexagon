package Pionnieers;

public class Jeton {
	private int nombreRestant;
	private int valeur;
	
	public Jeton(int nombreRestant, int valeur) {
		this.nombreRestant = nombreRestant;
		this.valeur = valeur;
	}
	
	
	
	/*Random rand = new Random(8);
	for(int j = 0 ; j < 2 ; j++){
		for(int i = 1 ; i < 13 ; i++)
			list.add(i);
	}
	list.remove(list.indexOf(1));*/
}
